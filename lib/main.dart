import 'package:flutter/material.dart';

main() => runApp(
  MaterialApp(
    title: 'FriendlyChat',
    home: Scaffold(
      appBar: AppBar(
        title: const Text('Friendly Chat'),
      ),
    ),
  )
);